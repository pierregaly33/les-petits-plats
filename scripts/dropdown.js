const boutonDropdown = document.querySelectorAll(".bouton_dropdown");
boutonDropdown.forEach((element) => {
    const input = document.createElement("input");
    element.appendChild(input);
});

function dropdownOpen() {}
dropdownOpen();
